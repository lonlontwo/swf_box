---
name: SWF 展示平台
description: 使用 OpenDrive 存儲 + Ruffle 播放器建立 SWF 展示庫，包含前台瀏覽與後台管理功能
---

# SWF 展示平台 Skill

## 📋 概述

一個簡潔的 SWF 作品展示平台，使用 OpenDrive 存儲檔案，Firebase Firestore 存儲清單數據，Ruffle 播放 SWF。支援任何設備訪問。

## 🎯 核心功能

### 前台 (index.html)
- 網格展示所有 SWF 作品（標題 + 縮圖）
- 點擊即可播放（開啟播放器頁面）
- 響應式設計，支援手機/平板/電腦

### 後台 (admin.html)
- 簡單密碼保護
- 新增 SWF（標題 + OpenDrive 連結）
- 編輯/刪除現有項目
- 即時同步到雲端

### 播放器 (player.html)
- 全螢幕 Ruffle 播放器
- 返回按鈕

## 🛠️ 技術架構

| 組件 | 技術 | 說明 |
|------|------|------|
| SWF 播放 | Ruffle CDN | `https://unpkg.com/@ruffle-rs/ruffle` |
| 數據存儲 | Firebase Firestore | 雲端同步、跨設備 |
| 檔案存儲 | OpenDrive | 免費 5GB，直連播放 |
| 前端 | HTML/CSS/JS | 純靜態網站 |
| 部署 | Cloudflare Pages | 免費託管 |

## 📁 專案結構

```
d:\swf展示平台\
├── index.html          # 前台 - 作品展示
├── admin.html          # 後台 - 管理介面
├── player.html         # 播放器頁面
├── css/
│   └── style.css       # 統一樣式
├── js/
│   ├── config.js       # Firebase 配置
│   ├── gallery.js      # 前台邏輯
│   ├── admin.js        # 後台邏輯
│   └── player.js       # 播放器邏輯
└── .agent/
    └── skills/
        └── swf-gallery/
            └── SKILL.md
```

## � 數據結構

### Firestore Collection: `swf_items`
```json
{
  "id": "auto-generated",
  "title": "作品標題",
  "swfUrl": "https://od.lk/s/xxxxx/file.swf",
  "thumbnail": "縮圖 URL（可選）",
  "createdAt": "timestamp"
}
```

### Firestore Collection: `settings`
```json
{
  "adminPassword": "your-password"
}
```

## 🔗 OpenDrive 直連

將 OpenDrive 分享連結轉為直連：
- 分享連結：`https://od.lk/f/xxxxx`
- 直連格式：`https://od.lk/s/xxxxx/filename.swf`

## 🎨 設計風格

深色主題 + 霓虹光效，現代遊戲館風格：
- 背景：深紫黑漸層
- 強調色：霓虹紫/藍
- 卡片：玻璃擬態 + hover 發光

## 🚀 開發步驟

// turbo-all

### 1. 建立樣式檔案
建立 `css/style.css` - 深色主題設計系統

### 2. 建立 Firebase 配置
建立 `js/config.js` - Firebase 初始化

### 3. 建立前台
建立 `index.html` + `js/gallery.js` - 作品展示庫

### 4. 建立播放器
建立 `player.html` + `js/player.js` - Ruffle 播放頁

### 5. 建立後台
建立 `admin.html` + `js/admin.js` - 管理介面

### 6. 測試與部署
```bash
npx -y serve .
```
